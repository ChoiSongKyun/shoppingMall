<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="scott">  
  <typeAlias alias="emp" type="sist.bean.EmpDto"/>
  <statement id="selectEmp" resultClass="emp" parameterClass="java.util.HashMap">
    SELECT EMPNO, ENAME, JOB, NVL(MGR,0)MGR, HIREDATE, SAL,
    NVL(COMM,0)COMM, DEPTNO
    FROM EMP    
    <dynamic prepend="WHERE">
      <isNotNull property="query">
          $query$ LIKE '%'||#data#||'%'
      </isNotNull>
    </dynamic> 
  </statement>
  <select id="selectEmpOne" parameterClass="java.lang.Integer" resultClass="emp">
    SELECT EMPNO, ENAME, JOB, NVL(MGR,0)MGR, HIREDATE, SAL,
    NVL(COMM,0)COMM, DEPTNO
    FROM EMP    
    WHERE EMPNO=#VALUE#  
  </select>
      
</sqlMap>







